import { render, screen } from "@testing-library/react"; import { QueryClient, QueryClientProvider } from "@tanstack/react-query"; import { MemoryRouter, Route, Routes } from "react-router-dom"; import { MeasurementResultPage } from "./MeasurementResultPage"; import * as api from "../api/measurements"; import { measurementId, scanId, succeededDetailFixture } from "../test/measurementFixtures";
vi.mock("../components/MeasurementPreview", () => ({ MeasurementPreview: () => <div>preview</div> }));
it("renders immutable result evidence from route IDs", async () => { vi.spyOn(api, "getMeasurementAttempt").mockResolvedValue(succeededDetailFixture); const client = new QueryClient({ defaultOptions: { queries: { retry: false } } }); render(<QueryClientProvider client={client}><MemoryRouter initialEntries={[`/scans/${scanId}/measurements/${measurementId}`]}><Routes><Route path="/scans/:scanId/measurements/:measurementId" element={<MeasurementResultPage />} /></Routes></MemoryRouter></QueryClientProvider>); expect(await screen.findByRole("heading", { name: "Measurement evidence" })).toBeInTheDocument(); expect(screen.getByText(/No AI, review, approval/)).toBeInTheDocument(); });
import { expect, it, vi } from "vitest";
